language: node_js
node_js:
 - "4.1.2"
script: npm test
before_install:
    - export DISPLAY=:99.0
    - sh -e /etc/init.d/xvfb start
before_script:
    - npm install
    #- if [ ! -f ./node_modules/jquery ]; then	mv ./node_modules/jquery ./node_modules/jQuery; fi
cache:
  directories:
    - node_modules
